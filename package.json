{
  "name": "freshsip-app",
  "version": "1.0.0",
  "description": "FreshSip Juice Store - MERN Stack Application - Production Ready",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "echo 'Backend deployment - no build step needed'",
    "start": "echo 'Starting server from root...' && cd server && npm start",
    "vercel-build": "npm run build",
    "postinstall": "echo 'Installing server dependencies...' && cd server && npm install",
    "test": "echo 'No tests configured'"
  },
  "engines": {
    "node": "22.x"
  },
  "type": "module"
}
