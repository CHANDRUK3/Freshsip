{
  "name": "freshsip-app",
  "version": "1.0.0",
  "description": "FreshSip Juice Store - MERN Stack Application",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "npm install --prefix client && cd client && npm run build",
    "start": "cd server && npm start",
    "vercel-build": "npm install --prefix client && cd client && npm run build"
  },
  "engines": {
    "node": ">=18.x"
  }
}
